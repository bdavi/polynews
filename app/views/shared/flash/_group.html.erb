<% flash.to_h.slice(*displayable_flash_types).each do |type, messages| %>
  <% Array(messages).each do |message| %>
    <%= render partial: 'shared/flash/message', locals: { type: type, message: message } %>
  <% end %>
<% end %>
