<%= simple_form_for @channel do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :url, hint: 'This must be a valid web address' %>
  <%= f.button :submit, class: 'btn btn-outline-primary' %>
<% end %>
